# Changelog

## v1.1.0

相对 v1.0.0，本版本重点升级 UI Designer、BMSUI 动态主题、资源占用、字体一致性和自定义 UI 稳定性。

### UI Designer

- 大尺寸编辑画布，并提供固定 320×240 的 1:1 实际尺寸预览。
- 所有元素支持拖动、八方向缩放和四边直接缩放；`Shift` 拖角保持比例。
- 框选多选、`Ctrl+点击` 增减选择、整组移动、`Ctrl+A`、方向键 1 px / 5 px 微调。
- 统一复制、剪切、粘贴、快速复制、删除、撤销和重做。
- 优化图层顺序、文字命中优先级、属性分组和实时双缓冲预览。
- 支持 Windows 系统字体、PNG/JPG、透明度、双状态图片、几何图形和渐变进度。
- 圆环进度只显示进度，中心数值改为独立动态数值元素。
- 文件流程统一为“新建 / 打开 / 保存 / 另存为 / 重置画布”。

### BMSUI v3–v7

- v3：静态系统字体资源、透明图片/图形、元素透明度和进度端点。
- v4：SOC 圆环和条形进度渐变。
- v5：`VSHAPE` 基础矢量图形，减少卡片类布局的位图内存占用。
- v6：`GLYPHVAL` 动态字形图集，解决字体、字距、粗细和温度符号上传后变化。
- v7：A8/RGB565/RGB565A8 自动图片编码、单色状态蒙版、资源去重、最小共享字形集和 `ASSETB64` 传输。
- 固件继续兼容读取 v1–v6 设备主题。

### ESP32-S3 固件

- 支持 ANT、JK、JBD、彦阳四类 BMS BLE 读取。
- 自定义 UI 采用暂存、校验、提交、重挂载验证的事务流程。
- 自定义 UI 启动失败自动回退 Factory UI，避免异常主题形成重启循环。
- 测试模式下优先处理 Web 请求，避免高频 UI 刷新影响配置和上传。
- 动态 UI 资源按 package asset 共享，避免多个控件重复分配同一图片/字形资源。
- LVGL 和动态 UI 大资源优先使用 PSRAM，不再使用固定 12/16 MiB 容量门槛；8 MB / 16 MB PSRAM 按运行时实际可用容量工作。
- `/api/ui/info` 提供资源预算和内存诊断信息。

### 代码与发布整理

- Designer 当前主题版本、Layout Schema 版本集中定义，消除重复常量。
- 新建空白画布统一使用当前 Schema 8，不再保留旧 Schema 6 遗留值。
- 固件版本统一为 `v1.1.0`，设备协议统一为 BMSUI v7。
- 删除开发阶段 Hotfix 文档和重复说明，重构 GitHub README、部署、协议、审计和发布文档。
- 普通源码注释保持极简；硬件/运行参数配置说明集中在 `firmware/src/app_config.h` 和部署文档。
- Windows 发布构建继续使用 `-trimpath -s -w -buildid=`。

## v1.0.0

上一正式版本，保留原 GitHub Tag/Release 作为历史基线。
